include: package:lints/recommended.yaml

# per https://dart.dev/guides/language/analysis-options#enabling-additional-type-checks
analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
#   exclude:
#     - path/to/excluded/files/**
